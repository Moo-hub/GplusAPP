"""
Minimal project stub.

This file is not the backend entry point. To start the API, run:
    python -m uvicorn backend.app.main:app --reload
"""

if __name__ == "__main__":
    print("Project stub. Start the backend via: python -m uvicorn backend.app.main:app --reload")
