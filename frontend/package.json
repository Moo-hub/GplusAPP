{
  "$schema": "https://json.schemastore.org/package.json",
  "type": "module",
  "name": "gplus-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --config ./vitest.config.js",
    "test:watch": "vitest --config ./vitest.config.js",
    "test:coverage": "vitest run --config ./vitest.config.js --coverage",
    "test:a11y": "vitest run --run a11y",
    "test:a11y:report": "node scripts/generate-a11y-report.js",
    "lint": "eslint src --ext .js,.jsx",
    "format": "prettier --write 'src/**/*.{js,jsx,css}'",
    "analyze": "vite build --mode analyze",
    "prepare-release": "npm run lint && npm run test && npm run test:a11y && npm run build",
    "smoke:run": "node -e \"const {spawnSync}=require('child_process'); const fs=require('fs'); const path=require('path'); const pkgPath=process.env.npm_package_json; let repoRoot; if(pkgPath){ repoRoot=path.resolve(path.dirname(pkgPath),'..'); } else { const cwd=process.cwd(); repoRoot = path.basename(cwd)==='frontend' ? path.resolve(cwd,'..') : cwd; } console.log('Running gated smoke tests (repo root:', repoRoot, ')'); const env=Object.assign({}, process.env, { RUN_E2E_SMOKE: 'true' }); const res=spawnSync('npx', ['vitest','run','--config','./vitest.config.js','--reporter','json'], { env, cwd: pkgPath ? path.dirname(pkgPath) : process.cwd(), encoding: 'utf8' }); if(res.error){ console.error(res.error); process.exit(1); } fs.writeFileSync(path.join(repoRoot,'frontend-full-results.json'), res.stdout || ''); console.log('Wrote report to', path.join(repoRoot,'frontend-full-results.json')); process.exit(res.status || 0);\""
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@tanstack/react-query": "^5.90.2",
    "axios": "^1.7.7",
    "i18next": "^23.15.0",
    "i18next-browser-languagedetector": "^8.0.0",
    "prop-types": "^15.8.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-i18next": "^15.0.2",
    "react-router-dom": "^6.26.2",
    "react-toastify": "^10.0.5",
    "styled-components": "^6.1.13",
    "web-vitals": "^4.2.3"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@testing-library/user-event": "^14.5.2",
    "@types/jest-axe": "^3.5.9",
    "@vitejs/plugin-react": "^4.3.2",
    "@vitest/coverage-v8": "^2.1.2",
    "cypress": "^13.15.0",
    "eslint": "^9.12.0",
    "eslint-plugin-react": "^7.37.1",
    "jest-axe": "^10.0.0",
    "jsdom": "^25.0.1",
    "msw": "^2.6.0",
    "prettier": "^3.3.3",
    "rollup-plugin-visualizer": "^5.12.0",
    "vite": "^5.4.8",
    "vite-plugin-pwa": "^0.20.5",
    "vitest": "^2.1.2"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }

  ,"optionalDependencies": {
    "canvas": "^3.2.0"
  }

}


