{
  "backup": "package.json backup",
  "note": "This is a safety copy created by automation before editing package.json"
}
{
  "name": "gplusapp",
  "version": "1.0.0",
  "description": "--- README.md+++ gplus-smart-builder-pro/README.md@@ -1,111 +0,0 @@---- README.md+++ gplus_smart_builder_pro/README.md@@ -1,102 +1,16 @@-# GPlusApp\r -+# GPlus Smart Builder Pro\r - \r --A full-stack web application with a FastAPI backend and React frontend, containerized with Docker and ready for production deployment.\r -+Welcome to your generated project!\r - \r -----\r -+## Getting Started\r - \r --## Features\r --- FastAPI backend (modular, SQLAlchemy, Alembic migrations)\r --- React frontend (Vite, modern JS)\r --- Docker & Docker Compose for orchestration\r --- CI/CD with GitHub Actions\r --- Environment variable management\r --- Logging and monitoring ready\r --\r -----\r --\r --## Quick Start\r --\r --### 1. Clone the repository\r --```sh\r --git clone https://github.com/Moo-hub/GplusAPP.git\r --cd GplusAPP\r --```\r --\r --### 2. Set up environment variables\r --- Copy `.env.example` to `.env` in both `gplus_smart_builder_pro/` and `frontend/` and fill in values.\r --\r --### 3. Build and run with Docker Compose\r --```sh\r --docker-compose up --build\r --```\r --\r --- Backend: http://localhost:8000\r --- Frontend: http://localhost:5173\r --\r --### 4. Run tests\r --- Backend:\r --   ```sh\r --   cd gplus_smart_builder_pro\r --   pytest\r --   ```\r --- Frontend:\r --   ```sh\r --   cd frontend\r --   npm test\r --   ```\r --\r -----\r --\r --## Deployment Checklist\r --- [ ] All tests passing (backend & frontend)\r --- [ ] Environment variables set (no secrets in code)\r --- [ ] Alembic migrations up to date\r --- [ ] Docker images build and run successfully\r --- [ ] CORS and security settings reviewed\r --- [ ] Logging and monitoring configured\r --- [ ] Production database configured\r --- [ ] HTTPS enabled in production\r --\r -----\r --\r --## Environment Variables\r --- See `.env.example` in each service for required variables.\r --\r -----\r --\r --## Monitoring & Logging\r --- Backend logs to `logs/gplus_smart_builder_pro.log` and console.\r --- Sentry integration available (set `SENTRY_DSN`).\r --- Frontend: Use `logError.js` for error reporting.\r --\r -----\r --\r --## License\r --MIT\r --\r --## Overview\r --This is a FastAPI backend project with modular structure for authentication, CRUD, database, and API routing.\r -+- Install dependencies: `pip install -r requirements.txt`\r -+- Run the app: `python app.py`\r - \r - ## Project Structure\r --- `gplus_smart_builder_pro/src/main.py`: FastAPI entry point\r --- `template/backend_fastapi/src/`: Backend modules (auth, crud, database, schemas, models, routes)\r --\r --## Setup\r --1. Install dependencies:\r --   ```pwsh\r --   pip install fastapi uvicorn sqlalchemy pydantic\r --   ```\r --2. Run the backend:\r --   ```pwsh\r --   python -m gplus_smart_builder_pro.src.main\r --   ```\r --3. API docs available at: [http://localhost:8000/docs](http://localhost:8000/docs)\r -+- `src/` - Main source code\r -+- `tests/` - Test suite\r -+- `docs/` - Documentation\r - \r - ## Next Steps\r --- Add more API routes in `routes/` and register them in `main.py`.\r --- Implement real business logic and database integration.\r --- Use git and GitHub for version control and collaboration.\r --\r --## Example Endpoints\r --- `/` - Root endpoint\r --- `/users` - Example user endpoint\r -+Start developing your application in the `src/` folder. Add tests in `tests/` and update documentation in `docs/`.",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cy:dev": "start-server-and-test dev http://localhost:3000 cypress:open",
    "cy:run": "start-server-and-test dev http://localhost:3000 cypress:run"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Moo-hub/GplusAPP.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Moo-hub/GplusAPP/issues"
  },
  "homepage": "https://github.com/Moo-hub/GplusAPP#readme",
  "devDependencies": {
    "@vitejs/plugin-react": "^2.1.0",
    "vite": "^3.1.0",
    "vite-plugin-pwa": "^0.13.1"
  },
  "type": "module",
  "dependencies": {
    "@fullcalendar/core": "^6.1.11",
    "@fullcalendar/daygrid": "^6.1.11",
    "@fullcalendar/interaction": "^6.1.11",
    "@fullcalendar/react": "^6.1.11",
    "@tanstack/react-query": "^5.90.2",
    "@tanstack/react-query-devtools": "^5.90.2",
    "axios": "^0.27.2",
    "i18next": "^25.5.2",
    "i18next-browser-languagedetector": "^8.2.0",
    "react": "^18.3.1",
    "react-calendar": "^4.9.1",
    "react-dom": "^18.3.1",
    "react-i18next": "^16.0.0",
    "react-query": "^3.39.2",
    "react-router-dom": "^6.30.1",
    "react-toastify": "^11.0.5"
  }
}
